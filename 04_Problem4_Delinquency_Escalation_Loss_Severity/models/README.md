# Models

This problem's "model" is a deterministic weighted severity score, not a trained black-box ML model -- see `src/severity_scorer.py` and `reports/validation_deployment/severity_scoring_bundle.json` (the real, measured, full-precision weights/means/stds/cutpoints) rather than a binary model file here.
