# Data

`risk_tier_assignments.csv` (if present above) is this platform's own real, computed output -- per-customer predicted PD and tier assignment on the real, held-out Kaggle test split. The raw Kaggle American Express Default Prediction competition CSVs themselves are not redistributed here -- see Problem 1's `GitHub_Repository_Package/data/README.md` (built by Notebook 18) for how to obtain them.
