# Known Lint Findings (Pre-Hardening Code)

`pyflakes` runs as an advisory (non-blocking) CI step as of 2026-08-24. It
was run once against the existing, already-verified `src/` files at the
time the CI workflow was added, to establish a baseline rather than let
pre-existing findings silently fail the very first CI run.

| File | Finding | Action |
|---|---|---|
| `Problem1_Credit_Scoring_PD_Prediction/src/monitoring/monitoring_job.py` | `joblib` imported but unused | Not fixed here — this file was generated by `12_monitoring.ipynb` and already pushed/verified; removing the import is a one-line, zero-risk fix but is left for a deliberate notebook-regeneration pass (see `ROADMAP.md`) rather than hand-edited out of band, so the notebook and the generated file never drift apart. |

Once every `src/` file under every `ProblemN_.../` has been swept the same
way (or the harmless findings above are cleared), flip `continue-on-error`
to `false` on the lint job in `.github/workflows/ci.yml` so lint failures
actually block merges.
