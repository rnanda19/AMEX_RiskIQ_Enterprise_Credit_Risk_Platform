# Dev/CI-only dependencies -- NOT needed to run the platform's own notebooks
# (each problem's own requirements.txt covers that). This file only supports
# the shared/ library's tests, each Problem's tests/, and the notebook
# syntax-check script.
pytest>=8.0
fastapi>=0.110
httpx>=0.27
scikit-learn>=1.4
joblib>=1.3
numpy>=1.26
pandas>=2.2
